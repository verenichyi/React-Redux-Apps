export const sqSize = 200;
export const strokeWidth = 10;
export const radius = (sqSize - strokeWidth) / 2;
export const viewBox = `0 0 ${sqSize} ${sqSize}`;
export const dashArray = radius * Math.PI * 2;