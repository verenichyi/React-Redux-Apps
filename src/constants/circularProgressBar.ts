export const sqSize: number = 200;
export const strokeWidth: number = 10;
export const radius: number = (sqSize - strokeWidth) / 2;
export const viewBox: string = `0 0 ${sqSize} ${sqSize}`;
export const dashArray: number = radius * Math.PI * 2;
